import * as React from 'react'
import Layout from '../components/layout'

const BlogPage = () => {
  return(
    <Layout pageTitle="The Latest">
      <p>My words will flood this page. Maybe it's nonsense, maybe it's profound.</p>
    </Layout>
  )
}

export default BlogPage